server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/jellyb3ar/SpringBoot-Config.git
          search-paths: cloud-config-file/**
          basedir: ./repo